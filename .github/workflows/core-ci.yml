name: core-ci

# CI dos componentes Python (vagg-core e vagg-license-server).

on:
  push:
    branches: [main]
    paths:
      - 'core/**'
      - 'license-server/**'
      - '.github/workflows/core-ci.yml'
      - 'Makefile'
  pull_request:
    paths:
      - 'core/**'
      - 'license-server/**'
      - '.github/workflows/core-ci.yml'
      - 'Makefile'

permissions:
  contents: read

concurrency:
  group: core-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  license-server:
    name: license-server (lint + test)
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: vagg
          POSTGRES_PASSWORD: vagg
          POSTGRES_DB: vagg_license_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U vagg -d vagg_license_test"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      VAGG_LICENSE_TEST_DATABASE_URL: postgresql+asyncpg://vagg:vagg@localhost:5432/vagg_license_test

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'license-server/pyproject.toml'

      - name: Install
        working-directory: license-server
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (ruff)
        working-directory: license-server
        run: |
          ruff check .
          ruff format --check .

      - name: Type check (mypy --strict)
        working-directory: license-server
        run: mypy --strict src

      - name: Migrations apply cleanly
        working-directory: license-server
        env:
          VAGG_LICENSE_DATABASE_URL: ${{ env.VAGG_LICENSE_TEST_DATABASE_URL }}
          VAGG_LICENSE_JWT_PRIVATE_KEY_PEM: "placeholder"
          VAGG_LICENSE_JWT_PUBLIC_KEY_PEM: "placeholder"
          VAGG_LICENSE_STRIPE_API_KEY: "sk_test_placeholder"
          VAGG_LICENSE_STRIPE_WEBHOOK_SECRET: "whsec_placeholder"
        run: alembic upgrade head

      - name: Tests
        working-directory: license-server
        run: pytest --cov=src --cov-report=term-missing --cov-report=xml --cov-fail-under=70

      - name: License compliance (pip-licenses)
        working-directory: license-server
        run: |
          pip install pip-licenses
          pip-licenses --format=plain --with-urls \
            --fail-on='AGPL;AGPL-3.0;AGPL-3.0-only;AGPL-3.0-or-later;GPL-3.0;GPL-3.0-only;GPL-3.0-or-later;SSPL;BUSL;BSL;FCL'

  core:
    name: vagg-core (lint + test)
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'core/pyproject.toml'

      - name: Install
        working-directory: core
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (ruff)
        working-directory: core
        run: |
          ruff check .
          ruff format --check .

      - name: Type check (mypy --strict)
        working-directory: core
        run: mypy --strict src

      - name: Migrations apply cleanly (SQLite tempfile)
        working-directory: core
        env:
          VAGG_CORE_DATABASE_URL: sqlite+aiosqlite:////tmp/vagg-core-ci.db
          VAGG_CORE_ADMIN_PASSWORD_HASH: "$argon2id$v=19$m=65536,t=3,p=4$placeholderhash"
          VAGG_CORE_JWT_SECRET: "placeholder-jwt-secret-for-migrations"
        run: alembic upgrade head

      - name: Tests
        working-directory: core
        run: pytest --cov=src --cov-report=term-missing --cov-report=xml --cov-fail-under=70

      - name: License compliance (pip-licenses)
        working-directory: core
        run: |
          pip install pip-licenses
          pip-licenses --format=plain --with-urls \
            --fail-on='AGPL;AGPL-3.0;AGPL-3.0-only;AGPL-3.0-or-later;GPL-3.0;GPL-3.0-only;GPL-3.0-or-later;SSPL;BUSL;BSL;FCL'
